// import create from "zustand";

// interface updateType {
//   newIndex: number;
//   setIndex: (i: number) => void;
// }

// const useUpdateStore = create<updateType>((set) => ({
//   newIndex: -1,
//   setIndex: (i) =>
//     set((state) => ({
//       newIndex: i,
//     })),
// }));

// export default useUpdateStore;
